# 9.30 作业
# 元祖：戴上来枷锁的列表
# 可以对元组进行操作的操作符：                                            拼接操作符：+                                                     重复操作符：*                                                     成员操作符：in/ not in                                            关系操作符：> <                                               逻辑操作符：and or
        
# 元组的内置函数：
# 比较两个元组的元素：operator.eq(temp1,temp2）（前提需import operator）
# 计算元组元素个数：len(temp1)
# 返回元组中元素最大值：max(temp1)
# 返回元组中元素最小值：min(temp1)
# 将列表转换为元组：tuple(list1)

